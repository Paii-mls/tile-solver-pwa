# Tile Solver PWA v10

Icon-only template detection. Canvas panning/dragging is disabled. Detection draws boxes around center icons only and filters out candidates that do not match the 13 templates above confidence threshold.
